CREATE TABLE member_types (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(50) NOT NULL
);

INSERT INTO member_types (name) VALUES ('일반'), ('프리미엄'), ('스페셜'), ('슈퍼관리자');

CREATE TABLE users (
    id VARCHAR(20) NOT NULL PRIMARY KEY,
    name VARCHAR(50) NOT NULL,
    email VARCHAR(100) NOT NULL,
    password VARCHAR(100) NOT NULL,
    phone_number VARCHAR(20),
    company_name VARCHAR(100),
    address VARCHAR(255),
    postal_code VARCHAR(20),
    member_type_id INT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    deleted_at TIMESTAMP NULL,
    FOREIGN KEY (member_type_id) REFERENCES member_types(id)
);

INSERT INTO users (name, email, password, phone_number, company_name, address, postal_code, member_type_id)
VALUES ('john_doe', 'john@example.com', '1234', '1234567890', 'ABC Company', '123 Main St, City, Country', '12345', 1);

